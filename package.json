{
  "name": "jsx",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun serve & bun compile --watch",
    "serve": "bun run serve.ts",
    "compile": "bun build src/example.tsx --outdir public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "preact": "^10.19.3"
  }
}