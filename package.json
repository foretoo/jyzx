{
  "name": "jsx",
  "module": "index.tsx",
  "type": "module",
  "scripts": {
    "dev": "bun serve & bun compile --watch",
    "serve": "bun --hot run serve.ts",
    "compile": "bun build index.tsx --outdir public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "preact": "^10.19.3"
  }
}